# ============================================
# DEVELOPMENT / PRODUCTION CONFIGURATION
# ============================================

# Use PORT provided by Render (or 8080 locally)
server.port=${PORT:8080}

# CORS Configuration (update with your frontend domain if needed)
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# JWT Secret
JWT_SECRET=YmVzdC10ZWNoLWJsb2ctc2VjcmV0LWtleS1mb3Itand0LWF1dGhlbnRpY2F0aW9uLXNwcmluZy1ib290LWFwcA==

# ============================================
# H2 DATABASE CONFIGURATION (temporary for Render test)
# ============================================
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# H2 Console (disabled in production, but accessible locally)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Logging (INFO for production)
logging.level.com.techblog.backend=INFO
logging.level.org.springframework.web=INFO